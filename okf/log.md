# Update Log

## 2026-06-22
* **Creation**: Added a top-level `README.md` (GitHub-flavored: shields, alerts, collapsible sections, a mermaid discovery diagram, key/flag/config tables) linking into this catalog. Published the repo at github.com/theesfeld/fokf.
* **Fix**: Audit follow-ups — `build_tree_view` group arrays are now dynamic (sized to `nconcepts`), removing the 128-distinct-type cap that could silently drop a concept (accuracy floor); selecting the `priority` group order in settings now prompts for the type list, so the mode is fully UI-configurable rather than config-file-only.
* **Update**: Collapse state now **persists across runs** — folds are stored in the config as `fold = bundle⇥group` lines keyed by bundle path (the single source of truth; the session-only array was deleted). Added `*` to **collapse/expand all** groups, and **configurable group order** (`group_order = type|count|priority` + `group_priority` list), cyclable in settings. Verified all three end-to-end.
* **Fix**: Hardened the collapse feature per audit — widened `collapsed_groups` to match `VRow.label` (a ≥64-char type name would otherwise never match in `is_collapsed`), and replaced the fragile pointer-identity reset with an explicit `load_bundle(dir, reset_collapse)` parameter (folds now reliably survive an in-bundle edit/new, reset only on bundle switch).
* **Update**: Tree groups are now **collapsible** (`Space`/`Enter` on a header, `▾`/`▸` markers) and show a **per-type count**; a selected header shows a folder summary in the content pane. Both panes sit in a **frame** whose focused pane is outlined/brightened with a highlighted `TREE`/`CONTENT` title. Collapse state is per-bundle session state.
* **Update**: Browser polish — left pane is now a **type-grouped tree** (headers per `type`, reserved files in their own group); added **pane focus** (`l`/`h` or arrows) so the content pane scrolls independently; fixed a screen-scroll bug where filling the bottom-right cell pushed the top bar off (status bar now stops one column short, `scrollok` pinned off). Editor discard is now a **single keypress**; `prompt_line` accepts **UTF-8** input; an `editor = internal|system` setting chooses fokf's editor or `$EDITOR`; discovery reads only the `index.md` prefix and the picker rescans only when roots change. Revised `tui-viewer.md`, `config.md`.
* **Fix**: Closed three v2 floor bugs from the audit — editor save failure no longer silently discards the buffer (keeps it, shows the error); open→save round trip is byte-identical (dropped the trailing-newline split artifact); `collect()` is depth-capped and skips symlinks (no stack overflow on symlink loops). All verified.
* **Update**: v2 — fokf is now a multi-bundle browser, editor, and scaffolder, not just a single-bundle viewer. Added: bundle **discovery** over XDG-configured search roots + a project→project **picker**; an in-TUI **editor** (`e`; codepoint-aware, atomic temp+rename save, `^O` save / `^X`-`Esc` cancel); **create** flows (`n` concept, `N` bundle; CLI `--new-bundle`/`--new-concept`) that seed conformant skeletons and prepend dated `log.md` entries; a **settings** screen and XDG **config** (`root`, `theme` default/bbs/mono, per-role `color.*`; unknown keys preserved); `?` **help** overlays; a BBS skin (menu bar, box-drawing banner, themed palette). New flags `--root`, `--help`. New concept docs: `discovery.md`, `editor.md`, `create.md`, `config.md`; revised `tui-viewer.md`, `build.md`, `index.md`. Fixed an overlapping-`snprintf` segfault in `load_bundle` and `^S` being eaten by tty flow control.
* **Update**: Inline spans now nest — `style_run`/`latex_run` recurse into bold/italic/link spans (layering attributes / wrapping LaTeX) so e.g. a code span inside bold renders, in both the TUI and the PDF export. Code spans stay literal; recursion is depth-capped (`INLINE_MAX_DEPTH`) against pathological nesting. Self-checked both paths.
* **Update**: Added soft-line-break reflow — a shared `read_block` joins lazy-continuation lines into one logical paragraph (bullets/blockquotes absorb their continuations) before inline styling, so emphasis spanning a soft break in the source renders in both the TUI and the PDF export. Self-checked.
* **Update**: Inline code now follows CommonMark backtick-run spans (`` `code` `` embeds single backticks) via a shared `code_span` helper; LaTeX export escapes backticks (`\textasciigrave`) so triple backticks no longer render as quote ligatures.
* **Update**: Applied PDF-exporter audit fixes — frontmatter URL values escaped unless a clean `\url` (closing an escaping-floor hole), `copy_file` now detects short-write/`fclose` errors and unlinks partial output, temp dir removed on the `fopen`-fail path. Switched fenced code from `verbatim` to `listings`/`lstlisting` (tolerates `\end{verbatim}` content; residual edge only on literal `\end{lstlisting}`).
* **Update**: Built the PDF exporter — `fokf --export-pdf <concept.md>` emits a `milmanual` (`~/.config/milstd`, §8 military standard) `.tex` and runs `pdflatex` (fork/exec, no shell, `-no-shell-escape`) in a temp dir, copying `./<name>.pdf` out. Reuses the shared block classifiers; headings→Section/`\milpar`, tables→booktabs (alignment honored), code→verbatim, links→`\href`, frontmatter→booktabs table. LaTeX escaping (with `\texttt` hyphen-break) self-checked via `open_memstream`. Extracted `classify_line`/`table_block` so the TUI and PDF paths share one classifier; `pdf-export.md` updated planned→built.
* **Update**: Tables now honor separator alignment colons (`:--` left, `--:` right, `:--:` center). Added cross-link following — in-bundle markdown links are listed in the bottom bar and jumped to with digit keys `1`-`9`. Added `col_align`/`resolve_link` self-checks; revised `tui-viewer.md`.
* **Creation**: Added `pdf-export.md` — planned-feature design for exporting a concept/bundle to PDF via tufte-latex (tufte-handout/tufte-book, booktabs, sidenotes). Not yet implemented.

* **Creation**: Seeded bundle root `index.md`, `log.md`, and concept docs `okf-format.md`, `tui-viewer.md`, `build.md` for the new fokf project (ncurses OKF viewer).
* **Update**: Implemented v1 — `fokf.c` + `Makefile`. Two-pane ncursesw viewer with a flat-YAML frontmatter parser, word-wrapped plain-text body, and a `--selftest` parser check. Revised `tui-viewer.md` to state v1 shows the body as wrapped plain text (markdown styling deferred).
* **Update**: Added body markdown styling (headings, inline code/bold/italic/links, fenced code, bullets, blockquotes; tables left literal) carried through word-wrapping as per-byte attributes, plus a `style_inline` self-check. Revised `tui-viewer.md` accordingly.
* **Update**: Added a color interface with capability detection (256-color palette when available, else 8/16-color) and a monochrome fallback forced by `--mono` / `--no-color` / `NO_COLOR`. Documented in `tui-viewer.md` and `build.md`.
* **Update**: Added pipe-table layout — tables now render as aligned columns (bold header, box-drawing rule, inline-styled cells) instead of literal source; left-aligned, wide tables clipped. Refactored `build_right` onto a line array for lookahead and made `style_inline` append-at-offset; added `is_table_sep`/`split_cells` self-checks. Revised `tui-viewer.md`.
